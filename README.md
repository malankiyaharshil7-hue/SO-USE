# SO USE
SO USE — Stop Over. Start Living.

## Vercel
Import this GitHub repository into Vercel.
Build command: `npm run build`
Output directory: `dist`

This packages the existing React interface as a PWA. Real Android usage tracking/blocking still requires native Android permissions/integration.
